import Vue from 'vue';
import TextareaAutosize from 'vue-textarea-autosize';

Vue.use(TextareaAutosize);

export default function (context) {
  // You can add additional configuration or setup here, if needed.
}
